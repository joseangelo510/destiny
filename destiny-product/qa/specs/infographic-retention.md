# Preserve existing infographic work on replacement failure

Existing plan, companion article, image URL and billing reservation remain available while replacement research runs and after it fails. Only a successful response replaces them; the old image is then released by the existing effect cleanup. Error remains visible. Do not alter reservations, claims, charging or provider replay behavior.

Supplemental browser test at repository-root qa/browser/infographic-retention.spec.ts uses intercepted research responses with a disposable signed-in fixture and local synthetic image-provider availability. Run on desktop and mobile against a production build. This focused fixture is not live provider or billing acceptance. Durable reload storage, expired reservation recovery and failed-render retry are still required separately for INFOGRAPHIC-03.
