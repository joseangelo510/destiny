export const INITIAL_PLAN_MONTHS = 3;
export const WEEKS_PER_PLAN_MONTH = 4;
export const INITIAL_PLAN_WEEKS = INITIAL_PLAN_MONTHS * WEEKS_PER_PLAN_MONTH;

// The customer may finish Keyword strategy after at least five approved
// keywords; they never need to review the whole recommendation pool.
export const INITIAL_KEYWORD_APPROVAL_TARGET = 5;
