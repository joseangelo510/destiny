Implement these production QA fixes with TDD, matching the intent precisely and without changing onboarding questions or the three-month plan.

1. Automatic editorial-calendar selection: keep the existing positive-volume default. Before any explicit user keyword decisions, also exclude likely competitor-branded junk-removal terms such as "loadup junk removal" and "junkman junk removal" unless the modifier is a normal service qualifier or an evidenced local place; exclude known onboarding competitor names; exclude "free" service terms unless the exact free service is actually offered; and exclude national/USA phrases for an evidenced Bay Area local service. Explicit user approvals must still override these automatic safeguards. Pass products/services, strategic-page location evidence, and known competitor names into the selection helper. Add a regression test using the real 98junkit failure set that keeps "commercial junk removal services" and "fremont junk removal" while excluding loadup, junkman, free junk removal, USA, and Ready Set Junk. Add an explicit-approval override test.

2. Article-generation truthfulness: derive availability and model label from the actual server environment. When ANTHROPIC_API_KEY is absent, do not advertise Claude Opus 4.8 as active and do not let the user click a button that will return 503. Show "Article model unavailable", disable the generation button with "Article generation is not configured", and explain that the starter outline can still be edited/downloaded while the model is connected. When configured, show the actual model label. Add a pure capability helper and unit tests.

3. Audit email truthfulness: on completed audits, read raw_provider_payload.emailDelivery. If it is skipped or failed, show a warning that results are saved in the product and the email update could not be sent. Do not claim delivery. The process-audit Edge Function is already deployed separately, so do not deploy Supabase from Replit; only add the web UI reading/rendering if it is not already present.

Run targeted tests first, then full tests, lint, TypeScript, and production build. Create a checkpoint. Do not publish; I will manually republish and verify.