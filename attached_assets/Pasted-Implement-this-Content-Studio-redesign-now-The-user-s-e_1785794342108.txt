Implement this Content Studio redesign now. The user's exact wording is authoritative and must be preserved in your reasoning:

"can you remove the editable starter outline from the content studio. its unnessasry and a user might think thats the actual post. so i will just remove. and after its done. maybe we can have the section show up or i dont know what s the best practice here. since it will be long article. i dont want it to way too long and look to awkward. consult with claude fable how to best approach or redesign with this instructions in mind and make sure to use my exact wording."

Claude Fable 5 recommended one state-driven design: Direction → Writing → Reading pane.

Implement that recommendation in article-review-workspace.tsx and related styles/tests:
1. Before generation, remove the editable starter outline completely. Do not show starter title, meta descriptions, body, word count, SEO fields, or an editable placeholder. Topic rail labels must say "Not written yet", not "Starter outline". Show one calm empty state: "Your article hasn't been written yet." and "Set the direction above, and Destiny will research your topic and write the full 2,000–3,000 word draft. Nothing here is placeholder — what you approve is what you publish." If generation is unavailable, use the existing truthful not-connected message and never fall back to an outline.
2. During generation, replace the empty state in place with a compact staged progress panel and subtle skeleton lines. Use truthful timed labels: Researching your topic…, Reading sources…, Writing your draft…, Polishing headings and metadata…. Keep direction controls visible but disabled/dimmed so the layout does not jump.
3. After a real generated draft exists, reveal the article in a fixed-height reading workspace (about 70vh) so a 2,000–3,000-word article never makes the whole page excessively long. Default to Preview: render the markdown as a readable document with proper headings, paragraphs, lists, links, and spacing. Add a Preview/Edit segmented control; Edit uses the existing textarea inside the same fixed-height scroll pane. Add an Expand/Collapse control. Show "Your article", word count, and estimated read time in the pane header. Include a compact H2 table of contents if it can be done cleanly without a fragile dependency.
4. Once generated, collapse the writing controls to a one-line settings summary with an "Adjust" control. Re-expanding allows edits; changed preferences keep the existing regenerate-required behavior.
5. Put SEO title and the two meta descriptions in a compact "Search listing" group after the reading pane, only after generation. Keep sources, infographics, quality checks, download, and approval behavior intact.
6. Do not change onboarding, keyword selection, editorial calendar, Anthropic request logic, or the 98junkit recommendations.
7. Add tests for all three states and the key copy/labels, run focused and full tests, lint, tsc, build, checkpoint, and do not deploy.