Apply the following narrowly scoped QA fixes to the existing Destiny SEO workspace. Do not change credentials, environment variables, authentication, domains, tenant data, Supabase project settings, deployment type, or unrelated features.

1. In src/components/weekly-task-list.tsx, hide “Skip for now” for both primary_quest and keyword_review; preserve it for other task kinds. Add/update its unit test.
2. In src/lib/product/roadmap.ts, make the three roadmap phases exactly Days 1–30, Days 31–60, and Days 61–90. Add/update its test and remove all 120/180-day roadmap copy.
3. Fix geographic keyword relevance in supabase/functions/process-audit/keyword-opportunity.ts and seo.ts: add page-text location evidence to the keyword business context; export a keywordHasGeographicConflict helper; reject non-ranking keyword candidates containing unsupported US city/metro phrases when those locations are absent from the website evidence; always preserve existing-ranking terms. Cover the 98junkit.com pattern so Fremont/Bay Area business evidence does not produce Los Angeles, Manhattan, Boston, Houston, Green Bay, Seattle, Chicago, or Philadelphia opportunities.
4. Fix editorial-calendar service relevance in src/lib/content/editorial-calendar.ts and src/app/content/page.tsx: add business products/services and parsed page text as context, score offer fit, filter out unsupported-location topics, and when at least 3 offer-anchored terms exist use those rather than audience-only phrases. For a junk-removal company serving property managers, “property managers near me/cost/rental property managers” must not become article topics; junk-removal service terms should. Add regression tests.
5. Apply the same geographic-conflict filter to persisted candidates displayed in src/app/keywords/page.tsx using live page evidence.
6. Make onboarding/loading email claims truthful in src/components/public-onboarding.tsx and src/components/audit-momentum-processing.tsx: say Destiny requests email updates and that the in-app notification center always contains the results/link; do not promise delivery. Add regression tests.
7. In supabase/functions/send-welcome/index.ts add structured console logging that distinguishes sent, skipped, and failed delivery. The Supabase Edge Function is already deployed as version 8; do not deploy Supabase.

Run npm test, npm run lint, npx tsc --noEmit, and npm run build. The validated local baseline is 61 test files / 223 tests. Resolve only release-caused failures. When every gate passes, republish the existing Reserved VM at https://destiny-seo.replit.app — do not create a new deployment or domain. Report exact test counts, build status, and new deployment identifier.